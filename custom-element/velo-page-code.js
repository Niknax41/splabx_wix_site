// Wix Studio / Velo page code
// Add a Custom Element and set the tag name to: splabx-site
// Attach the hosted script URL for custom-element/splabx-site.js in the element settings.

$w.onReady(function () {
  // Optional: use this page code for future interactions, CMS bindings, or analytics events.
});
